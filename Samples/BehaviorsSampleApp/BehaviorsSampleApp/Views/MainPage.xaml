<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Xamarin.Forms;assembly=Esri.ArcGISRuntime.Xamarin.Forms"
             xmlns:esrico="clr-namespace:EsriCo.ArcGISRuntime.Xamarin.Forms.UI;assembly=Esrico.ArcGISRuntime.Xamarin.Forms"
             x:Class="BehaviorsSampleApp.Views.MainPage"
             Title="{Binding Title}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <esriUI:MapView Map="{Binding Map}">
    </esriUI:MapView>
    <!--<esrico:LegendView Map="{Binding Map}" 
                       IsTitleVisible="true"
                       Title="Leyenda"
                       IsVisible="{Binding IsLegendVisible}" />
    <esrico:TableOfContentsView Map="{Binding Map}"
                                IsTitleVisible="true"
                                Title="Leyenda"
                                IsVisible="{Binding IsLegendVisible}" />-->
    <esrico:PanelView TitleText="Panel View"
                      StatusText="Status"
                      Margin="10"
                      CloseButtonLabel="Cerrar"
                      IsVisible="{Binding IsLegendVisible}">
      <esrico:PanelView.HeaderContent>
        <Label Text="Header" />
      </esrico:PanelView.HeaderContent>
      <esrico:PanelView.BodyContent>
        <Label Text="Body"
               HorizontalOptions="CenterAndExpand"
               VerticalOptions="CenterAndExpand" />
      </esrico:PanelView.BodyContent>
      <esrico:PanelView.FooterContent>
        <Label Text="Footer" />
      </esrico:PanelView.FooterContent>
    </esrico:PanelView>
    <StackLayout Grid.Row="1" 
                 Orientation="Horizontal">
      <Button Text="Tabla de Contenido"
              Command="{Binding TOCCommand}" />
      <!--<Button Text="Leyenda"
              Command="{Binding LegendCommand}" />-->
    </StackLayout>
  </Grid>
</ContentPage>