<?xml version="1.0" encoding="UTF-8"?>
<ui:LayerListView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:ui="clr-namespace:EsriCo.ArcGISRuntime.Xamarin.Forms.UI"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="EsriCo.ArcGISRuntime.Xamarin.Forms.UI.LayerLegendView">
  <ui:LayerListView.LayerItemTemplate>
    <DataTemplate>
      <ViewCell IsEnabled="False">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>
          <Image Grid.Column="0"
                 Source="{Binding ImageSource}"
                 Margin="5"
                 BackgroundColor="Transparent"
                 HorizontalOptions="CenterAndExpand"
                 VerticalOptions="CenterAndExpand" />
          <Label Grid.Column="1"
                 Text="{Binding Name}"
                 HorizontalTextAlignment="Start"
                 HorizontalOptions="StartAndExpand"
                 VerticalOptions="CenterAndExpand" />
        </Grid>
      </ViewCell>
    </DataTemplate>
  </ui:LayerListView.LayerItemTemplate>
</ui:LayerListView>